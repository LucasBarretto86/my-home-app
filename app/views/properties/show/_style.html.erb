<style>
    .cover {
        position: relative;
        top: 0;
        min-width: 100%;
        height: 50vh;
        margin-top: -32px;
        background-image: url("<%= url_for @property.cover unless @property.cover.nil? %>");
        background-size: cover;
        background-position: center;
        background-repeat: no-repeat;
    }

    .cover:after {
        position: absolute;
        content: '';
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        width: 100%;
        height: 100%;

        background-image: linear-gradient(to bottom, rgba(0, 0, 0, 0.0) 45%, rgba(0, 0, 0, 0.5) 100%);
        background-size: cover;
        background-repeat: no-repeat;
    }
</style>